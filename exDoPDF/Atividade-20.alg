algoritmo "impar ou par?"

    valor: inteiro

inicio

    escreval("Digite um número: ")
    leia(valor)

    se(valor % 2 = 0) entao
        escreva("Seu número é PAR!")
    senao
        escreva("Seu número é IMPAR")
    fimse

fimalgoritmo