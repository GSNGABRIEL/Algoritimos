algoritmo "Nome do Programa"
var

    dias, salario: real

inicio

    escreval("Quantos dias foi trabalhado? ")
    leia(dias)

    salario <- dias * 8 * 25

    escreva("Seu salário é: R$", salario)

fimalgoritmo