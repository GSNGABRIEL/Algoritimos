algoritmo "Operador Logico"
var

    A, B, C: inteiro

inicio
    A <- 2
    B <- 3
    C <- 5

    escreval(verdadeiro e falso)
    escreval((A=B) ou (C>A))
    escreval(nao((A=B) ou (C>A)))
   
fimalgoritmo