algoritmo "COntador de 0 a 10"
var

contador: inteiro

inicio
    contador <- 0
    enquanto (contador <= 10) faca
        escreval (contador)
        contador <- contador + 1
    fimenquanto

    escreva("Terminei de contar")

fimalgoritmo