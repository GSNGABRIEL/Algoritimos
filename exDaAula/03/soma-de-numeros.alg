algoritmo "Soma de numeros do usuario"
var

N1, N2, Soma: inteiro

inicio

    escreval("Informe um número: ")
    leia(N1)
    escreval("Informe um número: ")
    leia(N2)

    Soma <- N1 + N2

    escreva("A soma entre ",N1," e" ,N2," é igual à: ", Soma)

fimalgoritmo